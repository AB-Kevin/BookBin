name: Release

# Builds the Windows (.exe) and macOS (.dmg/.zip) installers and the Android
# APK, and publishes them all to the GitHub Release for the pushed tag.
#
# Usage:
#   npm version patch   # or minor / major — bumps package.json, commits, tags
#   git push --follow-tags
# This can be run from Windows (or anywhere) — GitHub's own macOS runner
# builds the Mac installer, no local Mac required.
#
# Two known electron-builder GitHub-publish gotchas this workflow works around:
#
# 1. windows and mac run one after another (not in parallel): electron-builder's
#    --publish step both creates/updates the same GitHub Release for the tag, and
#    two jobs doing that at the same time can race and silently drop an asset
#    (bit BillManager once — the Mac job's .dmg and latest-mac.yml never made it
#    into a release even though its job reported success). `needs:` below
#    serializes them so only one build job is ever touching the release at a time.
#
# 2. electron-builder itself has a known bug (electron-userland/electron-builder
#    #6676) where its "does a release already exist for this tag" lookup can
#    race/miss right after creating one, so it creates a SECOND release with the
#    same tag and splits assets across both. The create-release job below
#    pre-creates the release before either build runs, so both build jobs only
#    ever have to find-and-upload to one that already fully exists — no create
#    race left for electron-builder to lose.

on:
  push:
    tags:
      - "v*"
  workflow_dispatch: {}

permissions:
  contents: write

jobs:
  create-release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - name: Ensure a GitHub Release exists for this version
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          TAG="v$(node -p "require('./package.json').version")"
          if gh release view "$TAG" --repo "${{ github.repository }}" >/dev/null 2>&1; then
            echo "Release $TAG already exists, reusing it."
          else
            gh release create "$TAG" --repo "${{ github.repository }}" --title "$TAG" --notes ""
          fi

  release-windows:
    needs: create-release
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-node@v7
        with:
          node-version: 24
          cache: npm

      - run: npm ci

      - name: Build & publish (Windows)
        run: npm run release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_PUBLISHABLE_KEY: ${{ secrets.SUPABASE_PUBLISHABLE_KEY }}
          SUPABASE_ANON_KEY: ${{ secrets.SUPABASE_ANON_KEY }}

  release-mac:
    needs: release-windows
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-node@v7
        with:
          node-version: 24
          cache: npm

      - run: npm ci

      - name: Build & publish (macOS)
        run: npm run release:mac
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_PUBLISHABLE_KEY: ${{ secrets.SUPABASE_PUBLISHABLE_KEY }}
          SUPABASE_ANON_KEY: ${{ secrets.SUPABASE_ANON_KEY }}

      - name: Verify Mac auto-update assets were actually published
        run: |
          for f in dist/latest-mac.yml dist/*.dmg; do
            if ! ls $f >/dev/null 2>&1; then
              echo "::error::Expected artifact missing: $f (Mac release build produced no such file — check the electron-builder output above)"
              exit 1
            fi
          done

  # The Android APK. It is uploaded to the release with gh rather than
  # electron-builder's publisher, and the release already exists by now, so
  # this job has neither race to worry about and runs alongside the desktop
  # builds.
  #
  # Needs the signing key as repository secrets:
  #   ANDROID_KEYSTORE_BASE64    base64 of android/bookbin-release.jks
  #   ANDROID_KEYSTORE_PASSWORD  storePassword from android/keystore.properties
  #   ANDROID_KEY_ALIAS          keyAlias      (bookbin)
  #   ANDROID_KEY_PASSWORD       keyPassword
  release-android:
    needs: create-release
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-node@v7
        with:
          node-version: 24
          cache: npm

      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: 21
          cache: gradle

      - run: npm ci

      - name: Restore the signing key
        env:
          ANDROID_KEYSTORE_BASE64: ${{ secrets.ANDROID_KEYSTORE_BASE64 }}
        run: |
          if [ -z "$ANDROID_KEYSTORE_BASE64" ]; then
            echo "::error::The ANDROID_KEYSTORE_BASE64 repository secret is missing, so the APK cannot be signed."
            exit 1
          fi
          echo "$ANDROID_KEYSTORE_BASE64" | base64 --decode > "$RUNNER_TEMP/bookbin-release.jks"

      # chmod: a gradlew committed from Windows can lose its executable bit.
      - name: Build APK
        run: |
          chmod +x android/gradlew
          npm run android
        env:
          ANDROID_KEYSTORE_FILE: ${{ runner.temp }}/bookbin-release.jks
          ANDROID_KEYSTORE_PASSWORD: ${{ secrets.ANDROID_KEYSTORE_PASSWORD }}
          ANDROID_KEY_ALIAS: ${{ secrets.ANDROID_KEY_ALIAS }}
          ANDROID_KEY_PASSWORD: ${{ secrets.ANDROID_KEY_PASSWORD }}
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_PUBLISHABLE_KEY: ${{ secrets.SUPABASE_PUBLISHABLE_KEY }}
          SUPABASE_ANON_KEY: ${{ secrets.SUPABASE_ANON_KEY }}

      - name: Upload APK to the release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          VERSION="$(node -p "require('./package.json').version")"
          gh release upload "v$VERSION" "dist/BookBin-$VERSION.apk" --repo "${{ github.repository }}" --clobber
